#pragma once

// Mirrors the stronghold overlay to external click-through windows on
// non-game monitors so users can view it simultaneously on other displays.
void UpdateStrongholdCompanionOverlays();
void ShutdownStrongholdCompanionOverlays();
